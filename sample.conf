admin =   # Name of administrator user
allowMissingConfig = false  # Don't terminate the app if the ini file cannot be read.
allowUnknownFlags = false  # Don't terminate the app if ini file contains unknown flags.
captcha.coinhive =   # Site key for Coinhive captcha. Leave blank to disable.
captcha.hostile = false  # Make the captcha more hostile: require it at logins and when submitting posts. (Please don't do this.)
configUpdateInterval = 0s  # Update interval for re-reading config file set via -config flag. Zero disables config file re-reading.
css.style =   # File to use for CSS styles.
defaultuplinks = ,73.137.75.34:1942,64.52.84.87:1942  # Default uplink servers (use -defaultuplinks "" to disable)
disallowsignup = false  # Disallow signups (AUTHINFO USER signup:...)
hostname = misconfigured  # Hostname
html.about =   # File to use for about page template.
html.footer =   # File to use for footer template.
html.header =   # File to use for header template.
httplisten = :1943  # HTTP listen address
listen = :1942  # Listen address
sqldatabase = ./nntp.db  # SQL database path or url
sqlengine = sqlite3  # SQL database engine
uplinks =   # Uplink servers
